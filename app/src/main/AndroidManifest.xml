<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.faculdade.pa_ap2">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Activities.ImagesActivity"></activity>
        <activity android:name=".Activities.Learning.PlayVideoActivity" />
        <activity android:name=".Activities.Learning.ContactDetailsActivity" />
        <activity android:name=".Activities.Activities.NewContactActivity">
            <intent-filter>
                <action android:name="android.intent.action.NEWCONTACT" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.faculdade.pa_ap2.Provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity android:name=".Activities.Activities.ContactsActivity" />
        <activity android:name=".Activities.Activities.VideosActivity" />
        <activity android:name=".Activities.Activities.Mp3Activity" />
        <activity android:name=".Activities.Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.Activities.TestLibActivity" />
    </application>

</manifest>